# Controller environment template
# Copy/edit as needed or regenerate with: ./scripts/create-ctrl-env.sh --force

# Client baresip addresses (from infras/hosts.yml)
CLIENT_1_ADDR=localhost:4444
CLIENT_2_ADDR=localhost:4444

# Relay address (from infras/hosts.yml server/relay or localhost)
RELAY_ADDR=localhost:50052

# Redis configuration
REDIS_ADDR=localhost:6379
REDIS_USER=
REDIS_PASS=
REDIS_DB=0
REDIS_PREFIX=denseid:dia:peer_session:v1
PEER_SESSION_TTL=0

# Defaults
BARESIP_PORT=4444
RELAY_PORT=50052
DEFAULT_ODA_ATTRS=name,issuer
DEFAULT_CSV_DIR=./results
DEFAULT_INTER_ATTEMPT_MS=1000
