module_path         /usr/lib/baresip/modules

module              menu.so
module              account.so

# Codecs
module              g711.so
module              opus.so

# Headless audio
module              aubridge.so
module              aufile.so

# Optional automation/logging
module              ctrl_tcp.so
module              uuid.so
module              debug_cmd.so

ctrl_tcp_listen     0.0.0.0:4444
