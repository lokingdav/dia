module_path         /usr/lib/baresip/modules

# UI (optional but useful)
module              stdio.so
module              menu.so

# Codecs (required)
module              g711.so
module              opus.so

# Headless audio
module              aubridge.so
module              aufile.so

# Automation/control
module              ctrl_tcp.so
module              uuid.so
module              debug_cmd.so
