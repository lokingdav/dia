sip_accounts:
  - id: "account_id"
    name: "David Adei"
    password: "secret_password"
    # registrar:
    #   - "dia": use the Asterisk instance as the SIP server (via asterisk_externaddr)
    #   - otherwise: use the provided registrar hostname/IP (or "sip:host")
    registrar: "dia"
    client: "ansible_host_to_which_account_is_registered"
